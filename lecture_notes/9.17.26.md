# 9.17.26

1.3, 1.4, 2.2, 2.4 -> review these, network core, delays, http, dns
+ true or falses, short answer, short delay prob

**aux dns**
+ root dns records typically have ns records of tld servers, some A's
  + sometimes tld's have an ip address if the hostname for the auth server is ns.exactly_the_thing_we're_looking_for.com, we get stuck in an infinite nightmare loop.

## multi/demulti

multiplexing as sender -- add info to the header

demulti -- use header to deliver received sefs to correct socket

how do we know it is going to firefox? PORT NUMBER! just remember that port number is really tied to a process.

[interesting question that came up](https://stackoverflow.com/questions/1939707/how-two-browsers-simultaneously-listen-on-port-80)
+ port 80 is the http server port, but the CLIENT will have some random port assigned for listening for the reponse
  + auto assigned from the OS
+ netstat is a neat command for seeing this

TCP connection on an http server: server port stays 80, the distinction comes from the client port, IP distinction in the 4 tuple.
+ clients responsibility to choose different port numbers, NOT the server's

